<?xml version="1.0" encoding="utf-8"?>
<!-- Only the staged accounting export is ever shared out of this app. Scoped to that one cache
     subdirectory so no other app-private file can be handed to another app by mistake. -->
<paths>
    <cache-path name="export" path="export/" />
</paths>
